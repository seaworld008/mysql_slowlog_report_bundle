# MySQL慢日志分析工具 - 环境配置
# 请根据实际情况修改以下配置

# ========== MySQL慢日志配置 ==========
# 主机上慢日志文件目录（绝对路径，用于Docker挂载）
SLOWLOG_HOST_PATH=/var/log/mysql

# 容器内慢日志文件路径（支持通配符）
SLOWLOG_PATH=/app/slowlogs/slow*.log

# 分析结果TOP数量配置
TOP_DAILY=30        # 每天分析TOP数量
TOP_WEEKLY=50       # 每周分析TOP数量  
TOP_MONTHLY=100     # 每月分析TOP数量

# 最小执行时间阈值（秒，过滤掉执行时间短的查询）
MIN_TIME=1

# ========== Elasticsearch配置 ==========
# ES服务器地址（必须配置）
ES_HOST=http://your-es-server:9200

# ES认证信息
ES_USER=elastic
ES_PASSWORD=your_password

# 分析器主机标识（用于ES中区分不同服务器）
ANALYZER_HOSTNAME=mysql-prod-server-01

# ========== 高级配置 ==========
# 并发进程数（建议设置为CPU核心数）
JOBS=4

# 是否排除dump操作（true/false）
EXCLUDE_DUMPS=true
