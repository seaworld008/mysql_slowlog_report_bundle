# MySQL慢日志分析工具 - 开发环境配置
# 请根据实际情况修改以下配置

# ========== MySQL慢日志配置 ==========
# 主机上慢日志文件目录（绝对路径，用于Docker挂载）
SLOWLOG_HOST_PATH=./slowlogs

# 容器内慢日志文件路径（支持通配符）
SLOWLOG_PATH=/app/slowlogs/slow*.log

# 分析结果TOP数量配置（开发环境较小）
TOP_DAILY=10        # 每天分析TOP数量
TOP_WEEKLY=20       # 每周分析TOP数量  
TOP_MONTHLY=30      # 每月分析TOP数量

# 最小执行时间阈值（秒，开发环境设置较低）
MIN_TIME=0.5

# ========== Elasticsearch配置 ==========
# ES服务器地址（开发环境本地）
ES_HOST=http://localhost:9200

# ES认证信息（开发环境可能无认证）
ES_USER=
ES_PASSWORD=

# 分析器主机标识（开发环境标识）
ANALYZER_HOSTNAME=mysql-dev-analyzer

# ========== 高级配置 ==========
# 并发进程数（开发环境较小）
JOBS=2

# 是否排除dump操作
EXCLUDE_DUMPS=false
