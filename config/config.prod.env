# MySQL慢日志分析工具 - 生产环境配置
# 请根据实际情况修改以下配置

# ========== MySQL慢日志配置 ==========
# 主机上慢日志文件目录（绝对路径，用于Docker挂载）
SLOWLOG_HOST_PATH=/var/log/mysql

# 容器内慢日志文件路径（支持通配符）
SLOWLOG_PATH=/app/slowlogs/slow*.log

# 分析结果TOP数量配置（生产环境较大）
TOP_DAILY=50        # 每天分析TOP数量
TOP_WEEKLY=100      # 每周分析TOP数量  
TOP_MONTHLY=200     # 每月分析TOP数量

# 最小执行时间阈值（秒，生产环境过滤更严格）
MIN_TIME=2.0

# ========== Elasticsearch配置 ==========
# ES服务器地址（生产环境集群）
ES_HOST=https://your-prod-es-cluster:9200

# ES认证信息（生产环境必须配置）
ES_USER=elastic
ES_PASSWORD=your_production_password

# 分析器主机标识（生产环境服务器标识）
ANALYZER_HOSTNAME=mysql-prod-server-01

# ========== 高级配置 ==========
# 并发进程数（生产环境充分利用CPU）
JOBS=8

# 是否排除dump操作（生产环境建议排除）
EXCLUDE_DUMPS=true
